# TODO: There is no waybar integration in hyprland, so we could
#       display an overlay with the instructions while the mode is active.

submap=mode_screenshot

# MODE IMPLEMENTATION
# -------------------------------------

#BIND      MOD    KEY       DISPATCHER       VALUE
binde =    ,      s,        exec,      swayshot display upload
binde =    ,      r,        exec,      swayshot region upload
binde =    ,      w,        exec,      swayshot window upload
binde =    ,      a,        exec,      swayshot
binde =    ,      b,        exec,      swayshot region
binde =    ,      c,        exec,      swayshot window 


# Exit condition
#BIND      MOD    KEY       DISPATCHER       VALUE
bind =     ,      escape,   submap,          reset 

# -------------------------------------

submap=reset
