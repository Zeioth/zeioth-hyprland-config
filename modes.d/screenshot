# TODO: There is no waybar integration in hyprland, so we could
#       display an overlay with the instructions while the mode is active.

submap=mode_screenshot

# MODE IMPLEMENTATION
# -------------------------------------

#BIND      MOD    KEY       DISPATCHER       VALUE
bind =    ,      s,        exec,      swayshot display upload
bind =    ,      r,        exec,      swayshot region upload
bind =    ,      w,        exec,      swayshot window upload
bind =    ,      a,        exec,      swayshot
bind =    ,      b,        exec,      swayshot region
bind =    ,      c,        exec,      swayshot window 

# Exit condition
#BIND      MOD    KEY       DISPATCHER       VALUE
bind =     ,      escape,   submap,          reset 

# -------------------------------------

submap=reset
