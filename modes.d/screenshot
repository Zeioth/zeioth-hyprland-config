# TODO: There is no waybar integration in hyprland, so we could
#       display an overlay with the instructions while the mode is active.

submap=mode_screenshot

 # MODE IMPLEMENTATION
# -------------------------------------

#BIND      MOD    KEY       DISPATCHER       VALUE
bind =     ,      s,        exec,            hyprshotgun display upload
bind =     ,      r,        exec,            hyprshotgun region upload
bind =     ,      w,        exec,            hyprshotgun window upload
bind =     ,      d,        exec,            hyprshotgun alldisplays upload
bind =     ,      a,        exec,            hyprshotgun display
bind =     ,      b,        exec,            hyprshotgun region
bind =     ,      c,        exec,            hyprshotgun window 
bind =     ,      e,        exec,            hyprshotgun alldisplays

# Exit conditions
#BIND      MOD    KEY       DISPATCHER       VALUE
bind =     ,      escape,   submap,          reset 
bind =     ,      s,        submap,          reset 
bind =     ,      r,        submap,          reset 
bind =     ,      w,        submap,          reset 
bind =     ,      d,        submap,          reset 
bind =     ,      a,        submap,          reset 
bind =     ,      b,        submap,          reset 
bind =     ,      c,        submap,          reset 
bind =     ,      e,        submap,          reset 

# -------------------------------------

submap=reset
